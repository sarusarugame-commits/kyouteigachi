import os
import requests

def main():
    url = os.environ.get("DISCORD_WEBHOOK_URL")
    if not url:
        print("❌ エラー: Webhook URLが設定されていません")
        return

    print("🚀 テスト通知を送信します...")
    try:
        data = {"content": "🔔 **テスト通知**\nこれが届けば、Discordの設定は完璧です！"}
        res = requests.post(url, json=data)
        if res.status_code == 204:
            print("✅ 送信成功！Discordを確認してください。")
        else:
            print(f"❌ 送信失敗: ステータスコード {res.status_code}")
    except Exception as e:
        print(f"❌ エラー発生: {e}")

if __name__ == "__main__":
    main()
