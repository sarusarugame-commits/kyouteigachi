name: run-bot

on:
  # ⏰ スケジュール実行 (UTC時間で指定)
  # 日本時間 08:00 (UTC 23:00) 〜 23:30 (UTC 14:30) まで 30分おきに実行
  # ※Python側で「23:15以降は新規予想停止」の制御を入れているので、夜中に通知は来ません。
  schedule:
    - cron: '*/30 23,0-14 * * *'

  # 👇 手動実行ボタン (デバッグ用)
  # Actionsタブから「Run workflow」を押すと即座に動きます
  workflow_dispatch:

jobs:
  run-race-prediction:
    runs-on: ubuntu-latest
    timeout-minutes: 60 # フリーズ対策（60分で強制終了）

    steps:
      # 1. ソースコードを取得
      - uses: actions/checkout@v4
      
      # 2. Python 3.10 を準備
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      # 3. 必要なライブラリをインストール
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # discordwebhook は削除し、requests で直接送信するように変更しました
          pip install pandas numpy lightgbm requests beautifulsoup4 google-generativeai

      # 4. Botを実行 (環境変数を読み込む)
      - name: Run Main Script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python main.py
